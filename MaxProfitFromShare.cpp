//Maximum profit by buying and selling a share at most twice
//we will use the same approach as we done in max profit on buying and selling share on a single day

#include<bits/stdc++.h>
using namespace std;

int main(){
	int size;
	cin>>size;
	int arr[size];
	for(int i=0;i<size;i++)
	cin>>arr[i];
	int profit = INT_MIN;
	
}
